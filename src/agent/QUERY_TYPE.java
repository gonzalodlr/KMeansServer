package agent;

/**
 * Enumeration class called 'QUERY_TYPE' with two possible values: 'MIN' and
 * 'MAX'. An enumeration is a special type in Java that represents a fixed set
 * of constants. In this case, 'QUERY_TYPE' is used to specify whether a query
 * should return the minimum or maximum value.
 */
public enum QUERY_TYPE {
	MIN, MAX
}
